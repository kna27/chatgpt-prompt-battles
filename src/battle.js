/**
 * Runs a challenge
 * - 1 of more complicated functions
 * - Generate the "The password is BLAH" string on the fly ?
 * 
 * @param {Challenge} challenge The challenge being run
 * @param {User} user The user - attempt to the user's open ai key
 * @param {string} payload The actual prompt, see if it gets password
 */
async function runChallenge(user, challenge, payload) {
    // TODO(zsofia)
}

module.exports = {
    runChallenge
}